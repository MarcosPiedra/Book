<Window x:Class="Books.WPF.Views.EditBookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Books.WPF.Views"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        Title="" Height="240" Width="240">
    <StackPanel>
        <local:CardControl HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Command="{Binding AcceptCommand}" Margin="10">Accept</Button>
            <Button Command="{Binding CancelCommand}" Margin="10">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
